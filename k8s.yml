apiVersion: apps/v1
kind: Deployment
metadata:
  name: "pcc-puppet-enforcer-realm"
spec:
  selector:
    matchLabels:
      app: "pcc-puppet-enforcer-realm"
  template:
    metadata:
      labels:
        app: "pcc-puppet-enforcer-realm"
    spec:
      containers:
        - name: "pcc-puppet-enforcer-realm"
          image: "gcr.io/files/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "pcc-puppet-enforcer-realm"
spec:
  selector:
    app: "pcc-puppet-enforcer-realm"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080