micronaut:
  application:
    default-charset: UTF-8
    name: test-pcc-puppet-enforcer-realm
  config-client:
    enabled: false
  otel:
    enabled: false

tracing:
  jaeger:
    enabled: false

vault:
  client:
    config:
      enabled: false