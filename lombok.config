config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true
# Needed to correctly propagate annotations from Lombok to Micronaut
lombok.copyableAnnotations += io.micronaut.core.annotation.Nullable
